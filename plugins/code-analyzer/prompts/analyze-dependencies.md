You are a software architect.
Analyze the dependencies and relationships across these {{pathCount}} code files:
{{paths}}

Provide:
1. Import/dependency graph — which files depend on which
2. Shared utilities, types, or modules used across files
3. Circular dependencies (if any)
4. External package dependencies used
5. Coupling assessment — are files too tightly coupled?
6. Suggestions for improving the dependency structure

Present the dependency relationships clearly, using a diagram description or structured list.
