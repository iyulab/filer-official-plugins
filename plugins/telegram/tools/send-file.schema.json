{
  "type": "object",
  "properties": {
    "filePath": {
      "type": "string",
      "description": "Absolute path to the file to send"
    },
    "caption": {
      "type": "string",
      "description": "Optional caption for the file"
    },
    "chatId": {
      "type": "string",
      "description": "Optional. Target chat ID. Leave empty to use the pre-configured default chat ID."
    }
  },
  "required": ["filePath"]
}
